<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <meta name="description" content="{{ description }}"/>

    <meta property="og:title" content="{{ title }}"/>
    <meta property="og:type" content="article"/>
    <meta property="og:url" content="{{ page.url }}"/>
    <meta property="og:image" content="https://cdn.pixabay.com/photo/2015/03/26/09/43/building-690177_960_720.jpg"/>
    <meta property="og:description" content="{{ description }}"/>

    <style>
        :root {
            --bg-color: #033;
            --text-color: #EEE;
            --link-color: #EEE;
            --secondary-color: #CCC;
        }

        html {
            background: var(--bg-color);
            color: var(--text-color);
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            font-size: 105%;
            line-height: 1.5;
            padding: 2rem;
            font-variant-ligatures: none;
            text-rendering: optimizeSpeed;
            scrollbar-color: var(--bg-color);
            }

        @media screen and (min-width: 1024px) {
            html {
                font-size: 130%;
            }
        }

        body {
            max-width: 45rem;
            margin: auto;
        }

        header {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        main {
            margin: 5em 0;
        }

        header,
        footer {
            color: var(--secondary-color);
            font-size: .9rem;
        }

        footer {
            text-align: right;
        }

        h1 {
            font-size: 1.2rem;
            margin-bottom: 2rem;
        }

        a {
            color: var(--link-color);
        }
        
        div.conseil {
            display: flex;
            overflow-x: auto;
        }

        div.site {
            border: 2px solid var(--text-color);
            padding: .5rem;
            margin: 1rem;
            width: 10rem;
            flex-shrink: 0;
            font-size: .9rem;
        }

        .site h2 {
            margin: 0;
            padding: 0;
            font-size: 1rem;
            font-weight: 900;
        }

    </style>

</head>

<body>

    <header>
        reforme.ch
    </header>

    <main>
        <article>
            <h1>{{ title }}</h1>
            {{ content | safe }}
        </article>

        <div class="conseil">
        {% for site in conseil | shuffle | limit %}
        <div class="site">
            <h2><a href="{{site.url}}">{{ site.nom}}</a></h2>
            {{site.description}}
        </div>    
        {% endfor %}
        </div>
    </main>

    <footer>
        <p>site: 11ty | hébergement: netlify</p>
    </footer>

</body>

</html>